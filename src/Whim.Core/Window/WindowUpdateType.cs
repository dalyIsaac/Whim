namespace Whim.Core.Window
{
    public enum WindowUpdateType
    {
        Show,
        Hide,
        MinimizeStart,
        MinimizeEnd,
        Foreground,
        MoveStart,
        MoveEnd,
        Move,
    }
}